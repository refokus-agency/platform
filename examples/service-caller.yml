# Service caller — 2 ambientes (preview / production)
# Copiar este archivo a .github/workflows/ci-cd.yml en el repo de service.

name: CI + Deploy

on:
  push:
    branches: ['**']
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: refokus-agency/platform/.github/workflows/ci.yml@main
    secrets: inherit

  deploy-preview:
    needs: ci
    if: github.event_name == 'push' && github.ref != 'refs/heads/main'
    uses: refokus-agency/platform/.github/workflows/deploy.yml@main
    with:
      environment: preview
    secrets: inherit

  deploy-production:
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: refokus-agency/platform/.github/workflows/deploy.yml@main
    with:
      environment: production
    secrets: inherit
