# Custom-code caller — 3 environments (preview on PRs, stage on push to main, production on push to production).
# Copy this file to .github/workflows/ci-cd.yml in the custom-code repo.

name: CI + Deploy

on:
  push:
    branches: [main, production]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: refokus-agency/platform/.github/workflows/ci.yml@main
    secrets: inherit

  deploy-preview:
    needs: ci
    if: github.event_name == 'pull_request'
    uses: refokus-agency/platform/.github/workflows/deploy.yml@main
    with:
      environment: preview
    secrets: inherit

  deploy-stage:
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    uses: refokus-agency/platform/.github/workflows/deploy.yml@main
    with:
      environment: stage
    secrets: inherit

  deploy-production:
    needs: ci
    if: github.event_name == 'push' && github.ref == 'refs/heads/production'
    uses: refokus-agency/platform/.github/workflows/deploy.yml@main
    with:
      environment: production
    secrets: inherit
